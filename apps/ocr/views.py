"""API views for the shared OCR microservice (deal extract lives in ``deals.ocr_views``)."""

# Deal document extraction: POST /api/ocr/extract/ -> deals.ocr_views.extract_deal_document
# See api/urls.py and deals/urls.py (POST /deals/documents/extract/).
